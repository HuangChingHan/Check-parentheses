# Use Stack module to check if parenthese are pairs  
EX.  
if parenthese string is '(())', then the function will return "True".  
if parenthese string is '(((]))', then the function will return "False".  
